# Screenshot evidence

The requested screenshot matrix is documented in `../SCREENSHOT_INDEX.md`.

No valid StudyHub screenshot is currently part of the evidence set. The active browser automation session could not hold the staging tab and the generated candidate images were visually identified as unrelated tabs. They are therefore explicitly excluded from the audit evidence. Capture them again in a stable StudyHub browser session before using this package to make pixel-level redesign decisions.
